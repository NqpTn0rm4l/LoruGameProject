using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FrameRateRajoitin : MonoBehaviour
{
    public int kohdeFPS;
    // Start is called before the first frame update
    void Start()
    {
       // Application.targetFrameRate = kohdeFPS;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
