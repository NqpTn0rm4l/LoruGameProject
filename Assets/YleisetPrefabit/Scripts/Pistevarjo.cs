using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Pistevarjo : MonoBehaviour
{
    TextMesh txM;
    // Start is called before the first frame update
    void Start()
    {
        txM = GetComponent<TextMesh>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
